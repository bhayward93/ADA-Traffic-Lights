with Ada.Text_IO; use Ada.Text_IO;
package body lightswitcher is

   procedure TurnOn
     (dir : in     Direction;
      oct : in out Direction_Octet) is
   begin
    Put_Line("Inside of turnontraf");
      --HWIF."+"(1, 2);
     end TurnOn;
begin
    Put_Line("Inside of lightswitcher");
end lightswitcher;
